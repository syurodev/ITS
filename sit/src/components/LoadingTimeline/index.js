import classNames from "classnames/bind";

import style from "./LoadingTimeline.module.scss";

function LoadingTimeline() {
  const cx = classNames.bind(style);
  return <div></div>;
}

export default LoadingTimeline;
